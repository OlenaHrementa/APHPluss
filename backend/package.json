{
  "name": "aphplus-backend",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=14 <15",
    "npm": ">=6 <7"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint:js": "npx eslint \"src/**/*.{js,jsx}\" --resolve-plugins-relative-to .",
    "lint": "npm run lint:js"
  },
  "dependencies": {
    "express": "4.17.1"
  }
}
